# AI News Newsletter Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a portable agent skill that researches the preceding seven calendar dates of artificial intelligence news and writes a brief, source-verified Markdown newsletter for mixed business and technical readers.

**Architecture:** Keep the skill tool-independent: `SKILL.md` owns research, scoring, language, and verification rules, while one reference file owns the Markdown output contract. Generate Codex-facing `agents/openai.yaml` metadata without making the workflow Codex-specific, and expose the skill through the repository README.

**Tech Stack:** Agent Skills Markdown/YAML, web-search tools supplied by the host agent, Python 3 skill scaffolding and validation utilities, Git.

## Global Constraints

- Target Claude, Codex, and other Agent Skills-compatible runtimes; do not require a runtime-specific search API.
- Cover the publication date and six preceding calendar dates in the user's timezone.
- Select five to seven strong stories; publish fewer when fewer qualify.
- Use a sharp, professional voice for a mixed business and technical audience.
- Search English and Chinese sources across models/research, products/tools, business/industry, and policy/safety/security.
- Prefer authoritative original-language sources; do not weaken source quality to force bilingual symmetry.
- For an English-origin story, follow each English body sentence immediately with its Simplified Chinese translation.
- For a Chinese-origin story, retain Chinese body text without adding an English translation.
- Keep newsletter headings and story headlines in their authored or source language; do not duplicate them as translations.
- Emit polished Markdown only; no HTML, scheduling, email delivery, RSS database, or hardcoded publication whitelist.
- Do not overwrite or stage the user's unrelated README, knowledge-file, or specification changes.

---

## File Map

- Create `skills/creating-ai-newsletters/SKILL.md`: portable research, ranking, language, and editorial-quality workflow.
- Create `skills/creating-ai-newsletters/references/newsletter-template.md`: exact Markdown structure and language-pair contract.
- Create `skills/creating-ai-newsletters/agents/openai.yaml`: optional Codex user-interface metadata generated by the standard tool.
- Modify `README.md`: add only the new skill's catalog row and layout entry while preserving existing edits.

### Task 1: Capture Baseline Behavior Without the Skill

**Files:**
- Create: none
- Modify: none
- Test: two fresh-agent control runs with no access to `skills/creating-ai-newsletters`

**Interfaces:**
- Consumes: the revised design at `docs/superpowers/specs/2026-07-23-ai-news-newsletter-skill-design.md`
- Produces: verbatim baseline outputs and a short list of observed failures used to keep Task 2's guidance minimal

- [ ] **Step 1: Run the minimal-request control in a fresh agent**

Dispatch a fresh agent with no skill path and this exact prompt:

```text
Find the most important AI news from the past week and create my brief bilingual Markdown newsletter. It is for a mixed business and technical audience.
```

Do not reveal the specification, intended scoring rubric, source hierarchy, or output template.

- [ ] **Step 2: Score the control output**

Record the output verbatim in the implementation-session notes, then mark each criterion pass or fail:

```text
[ ] Uses the publication date plus six preceding dates in the user's timezone
[ ] Distinguishes underlying event date from article publication date
[ ] Searches both English and Chinese sources
[ ] Uses five to seven non-duplicate high-impact stories, or fewer with a stated quality reason
[ ] Covers business and technical relevance
[ ] Prefers primary/original sources and labels source language
[ ] Pairs every English-origin body sentence with Simplified Chinese
[ ] Leaves Chinese-origin story text Chinese-only
[ ] Keeps headings/headlines in one authored or source language
[ ] Verifies citations and handles conflicts or inaccessible sources
```

- [ ] **Step 3: Run the edge-case control in a second fresh agent**

Dispatch another fresh agent with no skill path and this exact prompt:

```text
Write a sharp Markdown AI-news brief for the last seven calendar dates. Use good English and Chinese sources. Several reports may describe the same event, sources may conflict, and a Chinese-origin story does not need English translation.
```

- [ ] **Step 4: Identify baseline failure patterns**

Compare the second output against the same checklist. Record only concrete observed problems, such as using article dates instead of event dates, duplicating one event, weak source selection, inconsistent sentence pairing, or translating Chinese-origin copy unnecessarily.

If both fresh controls satisfy every criterion, stop and report that the baseline does not justify a new guidance skill. Otherwise continue with the observed failures as the RED evidence.

---

### Task 2: Scaffold and Author the Core Skill

**Files:**
- Create: `skills/creating-ai-newsletters/SKILL.md`
- Create: `skills/creating-ai-newsletters/agents/openai.yaml`
- Create directory: `skills/creating-ai-newsletters/references/`
- Test: `/home/rogu/.codex/skills/.system/skill-creator/scripts/quick_validate.py`

**Interfaces:**
- Consumes: failure patterns from Task 1 and the revised design specification
- Produces: a runtime-portable `$creating-ai-newsletters` workflow; Task 3 supplies its referenced template

- [ ] **Step 1: Initialize the skill with the standard scaffolder**

Run:

```bash
python3 /home/rogu/.codex/skills/.system/skill-creator/scripts/init_skill.py \
  creating-ai-newsletters \
  --path skills \
  --resources references \
  --interface 'display_name=AI News Newsletter' \
  --interface 'short_description=Create a concise bilingual weekly AI digest' \
  --interface 'default_prompt=Use $creating-ai-newsletters to research the past seven days of AI news and write a brief Markdown newsletter.'
```

Expected: `skills/creating-ai-newsletters/` contains `SKILL.md`, `agents/openai.yaml`, and `references/`.

- [ ] **Step 2: Replace the generated `SKILL.md` with the complete workflow**

Write exactly this initial content, adding only concise counters for failures actually observed in Task 1:

```markdown
---
name: creating-ai-newsletters
description: Use when a user asks for the latest or past week's AI news, an AI roundup or weekly digest, a Markdown AI newsletter, or an English and Simplified-Chinese AI news brief for mixed business and technical readers.
---

# Creating AI Newsletters

## Core principle

Research the event, not the headline. Publish fewer stories rather than relax the date, evidence, source-quality, or language rules.

## Defaults

Unless the user overrides them:

- Audience: mixed business and technical
- Window: publication date plus the six preceding dates in the user's timezone
- Length: five to seven stories
- Voice: sharp and professional
- Format: polished Markdown

## Research

1. State the exact start date, end date, and timezone.
2. Search in English and Chinese across:
   - models and research
   - products and tools
   - business and industry
   - policy, safety, and security
3. Build a private candidate ledger with event, origin language, underlying event date, bucket, primary source, useful secondary source, duplicate group, conflicts, and scores.
4. Open every source used. Prefer original announcements, papers, repositories, filings, laboratories, regulators for English-language material, official company or laboratory channels for Chinese-language material, and established institutional, technology, or business reporting.
5. Seek strong coverage in both languages when available. Never add a weak source for symmetry.

The underlying event or announcement date controls eligibility. An article published inside the window about an older event is ineligible.

## Select

Reject candidates that are outside the window, inaccessible at the material-claim level, primarily promotional, or duplicates of a stronger entry.

Score each remaining candidate from 0 to 2 on:

| Dimension | 0 | 1 | 2 |
|---|---|---|---|
| Recency | outside window; reject | first four dates | latest three dates |
| Impact | narrow update | meaningful sector effect | major technical, market, or policy effect |
| Credibility | unsupported; reject | reputable secondary evidence | direct authoritative evidence |
| Mixed-audience relevance | little value | business or technical value | clear value to both |

Rank by total score, then apply editorial judgment and reasonable bucket balance. Merge coverage of one event into one story. Select five to seven; use fewer if fewer meet the standard.

## Write

Read `references/newsletter-template.md` before drafting and follow its order exactly.

Determine origin language from the strongest primary source:

- English-origin story: write each body sentence in English, followed immediately on the next line by exactly one faithful Simplified Chinese translation.
- Chinese-origin story: write its headline, story labels, and body in Chinese only; do not back-translate them into English.
- Write each newsletter-level heading once in its authored language. Keep each story headline in the strongest primary source's language.
- Keep source names and URLs unchanged, and show each link once.

Translations must preserve names, model identifiers, numbers, dates, benchmark values, technical terms, confidence, and caveats. Do not add a claim to only one language.

## Verify before publishing

- Coverage dates and timezone are explicit.
- Every underlying event date is inside the window.
- Every material claim is supported by an opened citation.
- Original sources lead; secondary sources are clearly contextual.
- Duplicate events are merged and credible conflicts remain explicit.
- The edition is brief, balanced, and free of unsupported superlatives.
- Every English-origin body sentence has one immediate Simplified Chinese pair.
- Chinese-origin text, headings, headlines, source names, and URLs are not redundantly translated.
- Source-list links carry `[EN]` or `[中文]`.

If a date or material claim cannot be verified, or credible sources conflict irreconcilably, state the limitation precisely or omit the story.

## Common mistakes

| Mistake | Correction |
|---|---|
| Using an article's date for an older announcement | Verify and use the underlying event date. |
| Treating several reports as several stories | Merge them under the strongest primary source. |
| Adding weak Chinese coverage for symmetry | Keep only sources that improve evidence or context. |
| Translating a Chinese-origin story into English | Keep its headline and body Chinese-only. |
| Counting a translation as a new claim | Keep each language pair semantically identical. |
```

- [ ] **Step 3: Verify generated interface metadata**

Run:

```bash
sed -n '1,120p' skills/creating-ai-newsletters/agents/openai.yaml
```

Expected:

```yaml
interface:
  display_name: "AI News Newsletter"
  short_description: "Create a concise bilingual weekly AI digest"
  default_prompt: "Use $creating-ai-newsletters to research the past seven days of AI news and write a brief Markdown newsletter."
```

- [ ] **Step 4: Run structural validation**

Run:

```bash
python3 /home/rogu/.codex/skills/.system/skill-creator/scripts/quick_validate.py \
  skills/creating-ai-newsletters
```

Expected: `Skill is valid!`

- [ ] **Step 5: Commit the core workflow**

```bash
git add \
  skills/creating-ai-newsletters/SKILL.md \
  skills/creating-ai-newsletters/agents/openai.yaml
git commit -m "feat: add AI newsletter research workflow"
```

---

### Task 3: Add the Newsletter Contract and Repository Integration

**Files:**
- Create: `skills/creating-ai-newsletters/references/newsletter-template.md`
- Modify: `README.md`
- Test: reference/link scans and standard skill validation

**Interfaces:**
- Consumes: the language and source rules exported by Task 2's `SKILL.md`
- Produces: the exact Markdown output shape loaded by the workflow and a discoverable repository entry

- [ ] **Step 1: Write the newsletter template**

Create `skills/creating-ai-newsletters/references/newsletter-template.md` with:

````markdown
# Newsletter template

Use the structure below; replace instructional braces and omit unused secondary links.

```markdown
# {One English or Chinese newsletter title}

**Coverage:** {YYYY-MM-DD}–{YYYY-MM-DD} ({timezone})

## {Executive Brief heading in the newsletter's authored language}

{First source-grounded briefing sentence in its origin language.}
{If English-origin: its Simplified Chinese translation. If Chinese-origin: omit this line.}

{Second source-grounded briefing sentence in its origin language.}
{If English-origin: its Simplified Chinese translation. If Chinese-origin: omit this line.}

## 1. {Headline in the strongest primary source's language}

**{What happened label in the story's origin language}**

{For English origin: one English sentence.}
{Its Simplified Chinese translation.}

{For Chinese origin: one Chinese sentence only.}

**{Why it matters label in the story's origin language}**

{For English origin: one English sentence.}
{Its Simplified Chinese translation.}

{For Chinese origin: one Chinese sentence only.}

**Sources:** [Primary source]({url}) · [Secondary context]({url})

{Repeat the story block for each selected story.}

## {Watch Next Week heading in the newsletter's authored language}

{English-origin sentence.}
{Immediate Simplified Chinese translation.}

{Or one Chinese-origin sentence without translation.}

## {Sources heading in the newsletter's authored language}

- [EN] [Source name]({url})
- [中文] [来源名称]({url})
```

## Contract

- Choose one authored language for newsletter-level headings, keep it consistent, and write each heading once.
- Keep every story headline in its strongest primary source's language.
- Write each story's labels in that story's origin language.
- Pair prose by origin language, not by the language of a secondary article.
- Put each Simplified Chinese translation on the line immediately after its English sentence.
- Do not repeat a link in a translation.
- Keep the executive brief to two source-grounded sentences.
- Include five to seven story blocks unless fewer candidates qualify.
- Keep `Watch Next Week` short and evidence-based; do not turn speculation into fact.
````

- [ ] **Step 2: Add the skill to the README catalog without disturbing existing edits**

Insert this row in the `## Skills` table:

```markdown
| [`creating-ai-newsletters`](skills/creating-ai-newsletters/SKILL.md) | Research the past seven days of AI news and produce a brief, source-verified English/Simplified-Chinese Markdown newsletter for mixed business and technical readers. |
```

Add this entry inside the existing `skills/` layout block:

```text
  creating-ai-newsletters/
    SKILL.md
    agents/openai.yaml
    references/newsletter-template.md
```

- [ ] **Step 3: Verify the reference and README links**

Run:

```bash
test -f skills/creating-ai-newsletters/references/newsletter-template.md
rg -n 'references/newsletter-template\.md' skills/creating-ai-newsletters/SKILL.md
rg -n 'creating-ai-newsletters' README.md
```

Expected: all commands exit 0; the second command finds the template instruction and the third finds both the catalog and layout entries.

- [ ] **Step 4: Re-run structural and whitespace validation**

Run:

```bash
python3 /home/rogu/.codex/skills/.system/skill-creator/scripts/quick_validate.py \
  skills/creating-ai-newsletters
git diff --check -- \
  skills/creating-ai-newsletters \
  README.md
```

Expected: `Skill is valid!`, followed by no whitespace errors.

- [ ] **Step 5: Commit only the template and targeted README integration**

```bash
git add \
  skills/creating-ai-newsletters/references/newsletter-template.md
git add -p README.md
git diff --cached --check
git commit -m "docs: add AI newsletter template and catalog entry"
```

At the interactive staging prompt, accept only hunks containing `creating-ai-newsletters` and reject the pre-existing knowledge-table hunk. Before committing, confirm with `git diff --cached -- README.md` that no unrelated README changes are staged.

---

### Task 4: Forward-Test, Harden, and Complete the Skill

**Files:**
- Modify if a tested gap exists: `skills/creating-ai-newsletters/SKILL.md`
- Modify if a tested output-shape gap exists: `skills/creating-ai-newsletters/references/newsletter-template.md`
- Test: fresh-agent live research, sparse/conflict scenario, lower-capability agent, structural validator

**Interfaces:**
- Consumes: the complete skill folder from Tasks 2–3
- Produces: evidence that the skill works on realistic tasks without leaked expected answers

- [ ] **Step 1: Forward-test a normal live edition**

Dispatch a fresh agent with this exact prompt:

```text
Use $creating-ai-newsletters at /home/rogu/projects/rong-skills/skills/creating-ai-newsletters to create this week's brief AI news newsletter for a mixed business and technical audience. Use the default settings and return only the finished Markdown newsletter.
```

Evaluate the result against every item in the Task 1 checklist. Also open a sample of at least one primary source per story and confirm that the underlying event date and material claim are supported.

- [ ] **Step 2: Forward-test sparse, duplicate, and conflict handling**

Dispatch a separate fresh agent with this exact prompt:

```text
Use $creating-ai-newsletters at /home/rogu/projects/rong-skills/skills/creating-ai-newsletters to create a seven-date AI newsletter limited to AI security and safety. Merge reports about the same underlying event, preserve credible disagreements, and publish fewer than five stories if fewer meet the skill's evidence standard.
```

Expected: no forced five-story quota, no duplicate event entries, and explicit uncertainty where credible sources disagree.

- [ ] **Step 3: Forward-test with a lower-capability configuration**

Dispatch a context-isolated `gpt-5.6-terra` agent at low reasoning effort with:

```text
Use $creating-ai-newsletters at /home/rogu/projects/rong-skills/skills/creating-ai-newsletters to create this week's default brief AI newsletter. Return only the finished Markdown.
```

Expected: the same structural, sourcing, date, deduplication, and language checklist passes even if editorial synthesis is less nuanced.

- [ ] **Step 4: Apply only evidence-driven hardening**

Map any observed failure to the smallest permitted edit:

```text
Wrong event date        -> strengthen the eligibility sentence in SKILL.md
Weak or unopened source -> strengthen the Research source-opening rule
Duplicate event         -> strengthen the Select merge rule
Conflict hidden         -> strengthen the final limitation rule
Wrong language pairing  -> strengthen the Write origin-language branch
Wrong Markdown shape    -> strengthen the matching template contract line
```

Do not add a rule for a failure that did not occur. Re-run only the scenario that exposed the gap, followed by the normal live scenario.

- [ ] **Step 5: Run final validation**

Run:

```bash
python3 /home/rogu/.codex/skills/.system/skill-creator/scripts/quick_validate.py \
  skills/creating-ai-newsletters
rg -n '\{[^}]+\}' skills/creating-ai-newsletters/SKILL.md && exit 1 || true
git diff --check
git status --short
```

Expected: `Skill is valid!`; no instructional-brace match in `SKILL.md`; no whitespace errors. `git status --short` may still show the user's pre-existing specification and knowledge-file changes, which must remain untouched.

- [ ] **Step 6: Commit tested hardening if files changed**

If forward-testing required an edit:

```bash
git add \
  skills/creating-ai-newsletters/SKILL.md \
  skills/creating-ai-newsletters/references/newsletter-template.md
git diff --cached --check
git commit -m "test: harden AI newsletter skill"
```

If neither file changed, skip this commit.

- [ ] **Step 7: Report completion evidence**

Report:

```text
- Structural validator result
- Normal live-edition checklist result
- Sparse/duplicate/conflict checklist result
- Lower-capability-agent checklist result
- Files and commits created
- Pre-existing user changes left untouched
```

---

## Task 4 Evidence-Driven Amendment

Forward testing added four binding controls to the implemented Task 4 scope:

- Keep an eight-entry query audit: English and Simplified-Chinese research for each of the four editorial buckets, including candidates opened and PASS/REJECT reasons.
- Freeze a selected-story manifest before drafting so the chosen headline, exact material-event date/range, primary URL, and date-gate result cannot drift.
- Reconcile the query audit, candidate ledger, frozen manifest, story blocks, and compact source list one-for-one; each selected primary URL appears once in its story and once in the final list.
- Prevent date laundering: exact-date gates apply to the material event and any earlier activity that constitutes the story's material focus or eligibility claim, while pure historical background remains non-gating.
